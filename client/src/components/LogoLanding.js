import logo from "../assets/images/logo-landing.png";

const Logo = () => {
  return (
    <img
      src={logo}
      alt="jobs for teachers"
      className="logo"
      // style={{ height: "50%", width: "50%" }}
    />
  );
};

export default Logo;
